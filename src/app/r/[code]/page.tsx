"use client";
import {useEffect,useState} from "react";import {useParams,useRouter,useSearchParams} from "next/navigation";import {resolveAffiliate} from "@/services/affiliate";import {saveReferralAttribution} from "@/lib/referral-attribution";
export default function ReferralLanding(){const {code}=useParams<{code:string}>(),router=useRouter(),search=useSearchParams(),[error,setError]=useState("");useEffect(()=>{if(!code)return;resolveAffiliate(code).then(()=>{if(!saveReferralAttribution(code,search.get("campaign")))throw new Error("Invalid referral");router.replace("/role");}).catch(()=>setError("This referral link is invalid or no longer active."));},[code,router,search]);return <main className="flex min-h-screen items-center justify-center bg-slate-50 p-6"><div className="max-w-md rounded-3xl border bg-white p-10 text-center shadow-sm"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-orange-50 text-3xl font-bold text-[#FF4B1F]">∞</div><h1 className="mt-5 text-2xl font-bold text-[#162B63]">Welcome to SlickHood</h1><p className="mt-2 text-slate-500">{error||"Confirming your referral and preparing account setup…"}</p>{error&&<button onClick={()=>router.replace("/role")} className="mt-6 rounded-xl bg-[#FF4B1F] px-5 py-3 font-semibold text-white">Continue without referral</button>}</div></main>}
